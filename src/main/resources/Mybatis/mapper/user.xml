<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.mapper.user">

    <select id="postLogIn" parameterType="signInVO" resultType="int">
        SELECT COUNT(name) FROM test_users WHERE name=#{name} AND password=#{password}
    </select>
    <select id="postUserMatch" parameterType="matchVO" resultType="int">
        SELECT COUNT(id) FROM test_users user, test_board board  WHERE user.name=board.name AND id=#{id} AND user.name=#{user}
    </select>
</mapper>
